type Query {
        DigimonInfo(id: Int!): Digimon!
        GetDigimons(id: Int!): [Digimon!]!
}

enum DigiType {
        VIRUS
        ANTIVIRUS
        DONNEES
}

type Digimon {
        id: Int!
        name: String!
        type: String!
        owner: User
}

